{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "root": "starrating-backend",
    "buildCommand": "mvn clean install -DskipTests",
    "startCommand": "java -jar target/*.jar --server.port=$PORT"
  }
}
